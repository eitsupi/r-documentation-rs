\name{rlike-raw-strings}
\title{R-like raw strings}

\usage{
paren <- r"(body } ] ) ' " % \ \} NEAR)x]y}z)"; after_paren()
upper <- R"(upper } body)"; after_upper()
nested <- r"((a))"; after_nested()
dash_near <- r"---(near )x)---"; after_dash_near()
}

\examples{
square <- r"[body } ] ) ' " % \ \} NEAR)x]y}z]"; after_square()
dash <- r"---(body } ] ) ' " % \ \} NEAR)x]y}z)---"; after_dash()
}

\description{\code{brace <- r"{body } ] ) ' " % \ \} NEAR)x]y}z}"; after_brace()}}

\alias{top}
